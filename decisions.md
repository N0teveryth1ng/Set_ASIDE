# Decisions Ledger — System Architecture Node

Every entry records: decision, rationale, alternatives considered, validation.

| # | Date | Decision | Why | Alternatives | Validation |
|---|------|----------|-----|--------------|-----------|
| D-001 | 2026-09-16 | v1 NORM® demo built as single static route `app/page.tsx` | Single-purpose demo; static output, zero runtime needs | Multi-route marketing site; client SPA | `npm run build` static, 200 on dev/start |
| D-002 | 2026-09-16 | v1 tokens `ink`/`bone` + `border-neutral-800` hairlines; duration-300 hovers | Consistent mono aesthetic | CSS vars; custom package | Build pass |
| D-003 | 2026-09-16 | Navbar routing extracted to `utilities/navConfig.ts` (typed) | Single source of truth | Inline array; CMS config | Build type-check clean |
| D-004 | 2026-09-16 | Detached dev-server lifecycle | Shell-bound jobs died between sessions | `Start-Job`; `next start` | Port 3000 verified |
| D-005 | 2026-09-16 | v1 Portfolio marquee grid (F-08) added | Feature request | Images; CMS portfolio | Build green |
| D-006 | 2026-09-16 | **PIVOT** — v1 NORM® demo superseded by **Creator Ledger** (premium Google Sheets funnel for creators). R-001 wiped `app/page.tsx` to Hello World boilerplate. F-01…F-08 deprecated. | Executive mandate: real product, high-converting landing, no SaaS backend | Keep v1; add second route | Doc-only pivot; R-001 build green |
| D-007 | 2026-09-16 | Token system re-pinned: `matte` `#0D0D0D`, `bone` `#F3F3F3`, `hairline` `#1A1A1A` | Premium matte-cream hierarchy fitting creator-product positioning | Retain old ink/bone hex | Phase 3 build verification |
| D-008 | 2026-09-16 | **Zero external packages**; all UI natively in `app/page.tsx` (Tailwind + lucide-react only) | Trivial deploys, top PageSpeed, zero dependency drift | Component-split tree; shadcn/ui | Bundle budget ≤100 kB |
| D-009 | 2026-09-16 | Hand-Off Matrix client copy: `navigator.clipboard` + transient `copied` state; page stays statically prerendered | No backend, instant access workflow | Desktop-only select-and-copy | Build green, static `/` |
| D-010 | 2026-09-16 | `utilities/navConfig.ts` orphaned post-R-001; removal deferred | Roadmap cleanliness; zero runtime impact | Immediate delete | Build green (unused)` |
| D-011 | 2026-09-16 | MVP-001 shipped: full Creator Ledger mono-grid landing built natively in app/page.tsx (Hero, PainPointMatrix, FeatureGrid, HandOffMatrix, Footer) + matte/bone/hairline tokens; ink retained as alias to #0D0D0D | Executive-approved Phase 2 build; zero-package single-file law; ink alias preserves globals.css base styles | Component-split tree; separate token removal | npm run build green, / static, 91.7 kB, zero placeholders |
| D-012 | 2026-09-16 | MVP-002: hardened Hand-Off Matrix — result-gated clipboard fallback (ok flag protects UI), high-contrast border-bone outline micro-feedback on module/button, explicit monospaced TOKEN_COPIED // ACCESS_READY chip rendered for exactly 2000 ms | Executive-tested interaction quality: blocked clipboard must never break UI and success must be unmistakable | Toast notification; persistent success banner | npm run build green, / static, single source match for TOKEN_COPIED |
| D-013 | 2026-09-16 | MVP-003: TEMPLATE_COPY_URL value switched to production forced-copy link https://google.com; constant name/mechanics untouched | Executive deployment directive; single source-of-truth constant keeps copy + feedback logic agnostic to asset URL | Inline URL literal at call sites | npm run build green, / static, stale URL grep = 0 |
| D-014 | 2026-09-16 | MVP-003-CORRECT: TEMPLATE_COPY_URL patched to corrected full-length production link (https://docs.google.com/spreadsheets/d/1VQpdYp_4q1hRHaKztEg8JSJjTNoBvdqjIjvyVR64dUc/edit?usp=sharing); prior value was a clipped placeholder from initial instructions | Manual executive correction of variable value; no logic or structural change | Leave prior google.com value | npm run build green, / static, grep = 1 line-18 match |
| D-015 | 2026-09-16 | MVP-004: TEMPLATE_COPY_URL suffix /edit?usp=sharing → /copy?usp=sharing (forced-copy conversion), line 18 only | Executive directive: access link must force a Drive template copy flow, not open the editor | Keep /edit editor view | npm run build green, / static, /edit grep = 0 |
| D-016 | 2026-09-16 | MVP-005: Hero stat strip → live interactive Income/Tax/ROI calculator — 4 numeric inputs bound to useState, escrow set-aside + brand ROI recompute in real time, values persisted in localStorage via usePersistedNumber (hydration-guarded) | Executive mandate: showcase the sheet's engine live, prove zero-backend interactivity, survive reloads | Server-rendered static only; external form lib | npm run build green, / static, storage keys + 4 inputs verified |
| D-017 | 2026-09-16 | MVP-006: isolated multi-page split — app/dashboard/page.tsx owns ALL calculator inputs, usePersistedNumber localStorage hooks, and real-time logic; app/page.tsx reduced to a stateless marketing funnel with the MVP-006 high-contrast Open Dashboard button (bg-bone) linking / → /dashboard | Executive finish-line: host workspace apart from conversion funnel; zero-package constraint preserved per route | Go /dashboard stayed single-page with scroll sections | npm run build green, / + /dashboard static, hook grep moved (0 in page.tsx, 4 keys in dashboard) |
| D-018 | 2026-09-16 | MVP-008: text input → parse pipeline — raw <textarea> log box + Commit Entry button call parseTransaction, append to LedgerEntry[], clear the field, and fire creator_ledger_v2_state JSON persistence; 5 raw unstyled total readouts render computeLedger output | Functional binding of the MVP-007 engine to user interaction; spec demands no styling on connection layer | Debounced auto-parse per keystroke; styled table view | npm run build green, / + /dashboard static, engine grep confirmed |
| D-019 | 2026-09-16 | FINAL-COMPLETION: core engine sprint — multi-line \n batch commit (one-sweep append), global escrow adjustment node (state default 23, instant metric recompute), deleteEntry(id) array-filter destruction, raw diagnostics panel with adjacent [Delete] buttons | Executive finish-line: closes the parsing/destruction feedback loop, zero backend, local-first | Per-line separate commits; styled table diagnostics | npm run build green, / + /dashboard static, engine grep confirmed |
| D-020 | 2026-09-17 | **MVP-013 Shadcn Full Dashboard Integration**: zero-package law scoped — `/dashboard` rebuilt on shadcn/ui (init Default/slate/CSS vars, v2.10 CLI; primitives card·button·input·textarea·toast + hooks/use-toast + lib/utils; deps cva/clsx/tailwind-merge/tailwindcss-animate); asymmetric 2-col split (LEFT Card console w/ Tax Escrow % Input + Textarea + Commit Button / RIGHT 5-metric card grid); Historical Diagnostics Card list w/ outline Delete → deleteEntry(id); engine preserved verbatim; landing stays zero-package native Tailwind | Enterprise primitives kill hand-rolled visual drift (Seapoint manual passes superseded); dark slate chassis bg-background/text-foreground | Keep single-file native engine; manual Seapoint grid | npm run build green: / 4.67 kB + /dashboard 12.4 kB static; dev :3000 PID 8692 both 200 |

Superseded rows D-001…D-005 preserved as historical audit. D-006 onward govern the Creator Ledger MVP.